apiVersion: v1
kind: Service
metadata:
  name: {{ include "mcp-oauth2-proxy.fullname" . }}
  labels:
    {{- include "mcp-oauth2-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mcp-oauth2-proxy.selectorLabels" . | nindent 4 }}
